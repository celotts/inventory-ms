// Archivo: infra/build.gradle

// Deshabilita la creación del JAR ejecutable de Spring Boot, resolviendo el error principal.
tasks.named('bootJar') {
    enabled = false
}

// Deshabilita la creación del JAR estándar.
tasks.named('jar') {
    enabled = false
}

// Deshabilita la compilación de código Java, ya que este módulo no lo contiene.
tasks.named('compileJava') {
    enabled = false
}